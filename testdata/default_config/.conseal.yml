# struct-packages not specified - all packages are targeted
# constructors not specified - default: "^New.*"
# ignore-files not specified - default: empty list
# allow-same-package not specified - default: false